#include <iostream>
#include <bits/stdc++.h>
#define MaxN 200000
using namespace std;
typedef long long ll;
int n;
struct Proj{
    ll st, ed, w;
} a[MaxN+5];
unordered_map<ll, ll> m;

void ReadData() {
    for (int i=1; i<=n; i++)
        cin >> a[i].st >> a[i].ed >> a[i].w;
}

void Compress() {
    for (int i=1; i<=n; i++) {
        m[a[i].st] = m.size() + 1;

    }
}

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
