#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int a[100001], n;

void ReadData() {
    for (int i=1; i<=n; i++) {
        cin >> a[i];
    }
}

int BinSearch(int lo, int hi, int val) {
    int m;
    while (lo <= hi) {
        m = (lo + hi)/2;
        if (a[m])
    }
}

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
