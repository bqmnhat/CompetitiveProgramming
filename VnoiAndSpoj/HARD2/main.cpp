#include <iostream>
#include <bits/stdc++.h>
#define MaxN 500
using namespace std;
int n, q, a[MaxN+1], b[MaxN+1];

void ReadData() {
    for (int i=1; i<=n; i++)
        cin >> a[i];
    for (int i=1; i<=n; i++)
        cin >> b[i];
}

void Solve() {
    for (int i=1; i<=q; i++) {
        int l, r, x;
        for (int j=l; j<=r; j++)
            a[i] = x;

    }
}

int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
