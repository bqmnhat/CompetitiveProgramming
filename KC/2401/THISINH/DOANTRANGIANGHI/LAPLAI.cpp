#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);
    freopen("LAPLAI.INP","r",stdin);
    freopen("LAPLAI.OUT","w", stdout);
    long long int n, m, A, B, T;
    cin >> T;
    while( T-- and 1<= T <= 50)
    {
        cin >> A >> B;
        if(A < B and 1 <= A <= B<= 2000000)
        {
            int c;
            c == 0;
            for(m=B-c)
            {
                std::to_string(m) << endl;
            }


        }

    }
}
